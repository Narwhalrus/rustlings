// variables4.rs
//
// Execute `rustlings hint variables4` or use the `hint` watch subcommand for a
// hint.

fn main() {
    let mut x = 3;
    let thing = 1..100;
    println!("Thing {:#?}", thing);
    println!("Number {}", x);
    x = 5; // don't change this line
    println!("Number {}", x);
}
